apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: agentic-o11y-mcp
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
