# TEMPLATE ONLY — do not commit real values.
# Create the real secret on the K3s VM with:
#
#   sudo kubectl create secret generic agentic-o11y-mcp-secrets \
#     --from-literal=splunk_o11y_token=<SPLUNK_O11Y_TOKEN> \
#     --from-literal=splunk_hec_token=<SPLUNK_HEC_TOKEN> \
#     -n agentic-o11y-mcp
#
apiVersion: v1
kind: Secret
metadata:
  name: agentic-o11y-mcp-secrets
  namespace: agentic-o11y-mcp
type: Opaque
stringData:
  splunk_o11y_token: "REPLACE_WITH_REAL_TOKEN"
  splunk_hec_token: "REPLACE_WITH_REAL_TOKEN"
