apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: agentic-o11y-mcp
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP
